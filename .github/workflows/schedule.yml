name: Scheduled Article Generation

on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:

jobs:
  generate_and_deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: 1. リポジトリをチェックアウト (生成スクリプト用)
        uses: actions/checkout@v4

      - name: 2. Python環境のセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # 🚨 修正ステップ: 仮想環境の作成とインストール、実行を統合 🚨
      - name: 3. 仮想環境作成、依存関係のインストール、スクリプト実行
        run: |
          # 仮想環境を作成
          python -m venv venv
          
          # 仮想環境内のpipを使って依存関係をインストール
          ./venv/bin/pip install google-genai pydantic -U
          
          # -----------------------------------------------------------
          # 🚨 最終修正: Python実行時にパスを強制的に追加 🚨
          # -----------------------------------------------------------
          
          # 仮想環境内の site-packages ディレクトリのパスを取得
          PYTHONPATH_VENV="./venv/lib/python3.11/site-packages"
          
          # Python実行時に -c オプションを使い、sys.path にパスを追加してからスクリプトを実行
          ./venv/bin/python -c "import sys; sys.path.append('$PYTHONPATH_VENV'); import generate_article; generate_article.main()"

        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

          
      - name: 4. 生成されたHTMLファイルの特定とデプロイ
        uses: cpina/github-action-push-to-another-repository@main
        env:
          API_TOKEN_GITHUB: ${{ secrets.DEPLOY_TOKEN }}
        with:
          source-directory: './' 
          destination-github-username: 'kiruha0315-debug' 
          destination-repository-name: 'kiruha0315-debug.github.io' 
          user-email: 'kiruha0315-debug@users.noreply.github.com'
          commit-message: 'feat: AIによる自動記事生成とデプロイ'
          target-branch: 'main'
          target-directory: 'articles'
          create-target-branch-if-needed: true
          target-files: '*.html'
