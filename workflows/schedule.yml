name: Scheduled Article Generation

on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:

jobs:
  generate_and_deploy:  # ジョブ名が generate_and_deploy に変更されています
    runs-on: ubuntu-latest
    
    steps:
      - name: 1. リポジトリをチェックアウト (生成スクリプト用)
        uses: actions/checkout@v4

      - name: 2. Python環境のセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 3. 依存関係のインストール
        run: pip install -r requirements.txt

      - name: 4. スクリプトの実行と記事の生成
        id: generate
        run: python generate_article.py
        env:
          # APIキーはSecretsで設定済み
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

      # --- 🚨 新規デプロイステップ 🚨 ---
      - name: 5. 生成されたHTMLファイルの特定とデプロイ
        uses: cpina/github-action-push-to-another-repository@main
        env:
          # ステップ3.2で設定したトークンを使用
          API_TOKEN_GITHUB: ${{ secrets.DEPLOY_TOKEN }}
        with:
          source-directory: './' 
          destination-github-username: 'kiruha0315-debug' 
          destination-repository-name: 'kiruha0315-debug.github.io' # 公開サイトのリポジトリ
          user-email: 'kiruha0315-debug@users.noreply.github.com' # あなたのGitHub登録メール
          commit-message: 'feat: AIによる自動記事生成とデプロイ'
          target-branch: 'main'
          target-directory: 'articles' # 公開サイトの articles/ フォルダに配置
          create-target-branch-if-needed: true
          target-files: '*.html'
